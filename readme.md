# StayEasy API 🏨

โปรเจกต์นี้คือการสร้าง RESTful API สำหรับระบบจองห้องพัก/โรงแรมอย่างง่าย (StayEasy) โดยใช้ Java Spring Boot พัฒนาโดยมี AI Agent (Gemini) เป็นผู้ช่วยในการพัฒนาและให้คำแนะนำ

## 🎯 เป้าหมายของโปรเจกต์
สร้าง Backend API ที่มีความปลอดภัย (Secure), ทดสอบได้ (Testable) และขยายผลได้ (Scalable) เพื่อรองรับฟังก์ชันการทำงานหลักๆ ของระบบจองที่พัก

---

## 🚀 Tech Stack
* **Language:** Java 17+
* **Framework:** Spring Boot 3+
* **Core:** Spring Web, Spring Security
* **Data:** Spring Data JPA
* **Database:** H2 (In-Memory) (สำหรับการพัฒนา) / PostgreSQL (สำหรับ Production)
* **Security:** JWT (JSON Web Token)
* **Build Tool:** Maven
* **Testing:** JUnit 5, Mockito

---

## 🗺️ ขั้นตอนการพัฒนา (Project Steps)

นี่คือขั้นตอนหลักที่เราจะทำเพื่อสร้างโปรเจกต์นี้ให้สำเร็จ:

### ขั้นตอนที่ 1: การตั้งค่า (Setup) - (✅ เสร็จสิ้น)
1.  **Generate Project:** สร้างโครงสร้างโปรเจกต์ด้วย Spring Initializr
2.  **Add Dependencies:** เพิ่ม Dependencies ที่จำเป็นใน `pom.xml`
3.  **Git & GitHub:** เริ่มต้น Git, สร้าง `.gitignore`, และ Push โครงโปรเจกต์

### ขั้นตอนที่ 2: การสร้างแบบจำลองข้อมูล (Data Models) - (✅ เสร็จสิ้น)
1.  **Create Entities:** `User.java`, `Role.java`, `Room.java`, `Booking.java`
2.  **Repositories:** `UserRepository`, `RoomRepository`, `BookingRepository`
3.  **Database Config:** ตั้งค่า `application.properties` (สำหรับ H2)

### ขั้นตอนที่ 3: การตั้งค่าระบบความปลอดภัย (Security) - (✅ เสร็จสิ้น)
1.  **User Service:** สร้าง `UserDetailsServiceImpl`
2.  **JWT Utilities:** สร้าง `JwtTokenProvider` และ `JwtAuthenticationFilter`
3.  **Security Config:** สร้าง `SecurityConfig` (กำหนดกฎการเข้าถึง, PasswordEncoder, AuthManager)

### ขั้นตอนที่ 4: การสร้างฟีเจอร์หลัก (Business Logic) - (กำลังดำเนินการ ⏳)
1.  **DTOs:** สร้าง `RegisterRequest`, `LoginRequest`, `AuthResponse`
2.  **Auth Service:** สร้าง `AuthService` (Logic การ Register/Login)
3.  **Auth Controller:** สร้าง `AuthController` (เปิด API `/api/auth/**`)
4.  **Admin Features:** (ขั้นตอนต่อไป) สร้าง API สำหรับ Admin (`/api/rooms/**`)
5.  **User Features:** (อนาคต) สร้าง API สำหรับ User (`/api/bookings/**`)

### ขั้นตอนที่ 5: การทดสอบ (Testing)
1.  **Unit Tests:** เขียน Unit Tests สำหรับ Service classes
2.  **Integration Tests:** เขียน Integration Tests สำหรับ API Endpoints

---

## ⚙️ วิธีการรันโปรเจกต์ (How to Run)

1.  Clone a project
    ```bash
    git clone [https://github.com/](https://github.com/)[YourUsername]/stayeasy-api.git
    ```
2.  (โปรเจกต์นี้ใช้ H2 In-Memory Database ไม่ต้องตั้งค่า Database เพิ่ม)
3.  รันแอปพลิเคชัน
    ```bash
    mvn spring-boot:run
    ```

---
*เอกสารนี้สร้างและดูแลโดย AI Agent (Gemini) ประจำโปรเจกต์*